DEPRECATED - DO NOT USE

This legacy React frontend is no longer served or referenced by the API. The authoritative UI is `ui_v2`. The `app/` directory remains only as an archive and will be removed in a future cleanup pass.
