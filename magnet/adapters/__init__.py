"""
Adapter layers between subsystems.

Adapters translate between kernel exports (geometry/observables/validation) and
consumer needs (rendering, UI, external formats) without forcing the kernel to
import consumer modules.
"""

